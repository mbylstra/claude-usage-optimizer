---
description: Git commit the currently staged files
allowed-tools: Bash(git status:*), Bash(git diff:*), Bash(git commit:*)
---
Git commit the currently staged files.

Current status:
!`git status`

Staged diff:
!`git diff --cached`

Write a commit message that reflects the staged changes and run `git commit`. Do not stage additional files.
